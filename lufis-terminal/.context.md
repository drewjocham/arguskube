# Argus Terminal — Complete

GPU-accelerated terminal for software engineers. Go module `github.com/argus/terminal`.

## Stats

```
46 Go files | 7,572 total lines (4,989 source + 2,583 test) | 15 test suites | all green
```

## Phases

### Phase 1 — Terminal Core ✅
| Package | Tests | Purpose |
|---------|-------|---------|
| `cmd/argus-terminal` | — | Entry point, GLFW window, keyboard, render loop |
| `internal/ansi` | 38 | Full VT100 state machine parser (SGR, CSI, OSC) |
| `internal/config` | 11 | TOML config + env var overrides, atomic save |
| `internal/pty` | 13 | PTY lifecycle (Start/Write/Resize/Close) |
| `internal/render` | — | Freetype font + GL window (CPU→GPU display pipeline) |
| `internal/screen` | 28 | Cell buffer, scrollback ring buffer, cursor, colors |
| `internal/opencode` | — | AI runtime with LLM client + ToolSet interface |
| `internal/auth` | 6 | Credential store with service detection |
| `internal/notes` | 11 | JSON-backed note store with search/tags/persistence |

### Phase 2 — Features ✅
| Package | Tests | Purpose |
|---------|-------|---------|
| `internal/automate` | 6 | Trigger-condition-action pipeline engine |
| `internal/blocks` | 9 | Command blocks (save/rerun/edit/search) |
| `internal/editor` | 8 | File editor with tabs, syntax highlighting, search |
| `internal/git` | 6 | Git status, branches, log, diff, blame, commit |
| `internal/layout` | 7 | Split panes (horizontal/vertical) |
| `internal/obsidian` | 3 | Vault discovery, note parsing, tags, wikilinks |
| `internal/recorder` | 8 | Meeting audio recording with history |
| `internal/session` | 4 | Tab/pane state persistence |
| `internal/shell` | 5 | Preexec/precmd hooks, timing, exit code |

### Phase 3 — Plugin System ✅
| Package | Tests | Purpose |
|---------|-------|---------|
| `internal/plugin` | — | Plugin manager (registry, lifecycle, hooks) |
| `plugin/api` | — | Plugin API: HostAPI, commands, panels, hooks |
| `plugins/argus-kube` | — | K8s cluster status, pod list, alert sidebar |
| `plugins/db-diag` | — | Database diagnostics (slow queries, indexes) |
| `plugins/infra-diag` | — | Infrastructure diagnostics (DNS, TLS, network) |

### Phase 4 — Community (skeleton) ✅
Infrastructure for plugin registry and store is in place via `internal/plugin`.

### Phase 5 — Polish & Scale (skeleton) ✅
| Package | Tests | Purpose |
|---------|-------|---------|
| `internal/i18n` | — | Multi-language support (en/de/fr/es/ja/zh) |
| `internal/crash` | — | Crash report capture, stack traces, persistence |
| `internal/security` | — | AES-256-GCM encrypted vault for credentials |
| `internal/update` | — | Auto-update infrastructure |
