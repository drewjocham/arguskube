apiVersion: v1
kind: Service
metadata:
  name: {{ include "argus-runner.fullname" . }}
  labels:
    {{- include "argus-runner.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "argus-runner.selectorLabels" . | nindent 4 }}
