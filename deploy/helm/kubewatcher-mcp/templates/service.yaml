apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubewatcher-mcp.fullname" . }}
  labels:
    {{- include "kubewatcher-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type | default "ClusterIP" }}
  ports:
    - port: {{ .Values.service.port | default 8080 }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kubewatcher-mcp.selectorLabels" . | nindent 4 }}
